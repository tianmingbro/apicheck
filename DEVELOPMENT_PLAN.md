# KEYPILOT 商业化开发计划

> 基于 `2026-06-11` 全面审计结果制定

---

## 总览

| 阶段 | 目标 | 工期 | 任务数 |
|------|------|------|--------|
| **Phase 0** — 安全加固 | 消除泄露风险，加固基础设施 | 1 天 | 6 |
| **Phase 1** — 核心闭环 | 打通前后端完整链路，可演示 | 3-5 天 | 8 |
| **Phase 2** — 功能补全 | 管理员后台、支付集成、提醒机制 | 5-7 天 | 7 |
| **Phase 3** — 生产就绪 | 迁移、监控、容灾、压测 | 3-5 天 | 5 |
| **Phase 4** — 体验优化 | 性能、UX、文档 | 持续 | 4 |

---

## Phase 0：安全加固（优先级最高）

> **目标**：消除已泄露的密钥和配置风险，防止安全事故。

### 任务清单

| # | 任务 | 当前状态 | 方案 | 估时 |
|---|------|----------|------|------|
| 0.1 | **轮换所有密钥** | `.env` / `docker-compose.yml` 含明文密钥 | 生成新 JWT_SECRET_KEY、ENCRYPTION_KEY、Postgres/Redis 密码；支付宝密钥重新生成 | 2h |
| 0.2 | **清理 `.env` Git 历史** | 敏感文件已跟踪 | `git rm --cached .env` + `.gitignore` 强制忽略 + BFG 清理历史 | 1h |
| 0.3 | **移除 SSL 私钥** | `nginx/ssl/` 含 `.key` 文件 | 从仓库删除，部署时通过 CI/CD 注入或挂载 volume | 0.5h |
| 0.4 | **环境变量注入** | `docker-compose.yml` 硬编码密钥 | 统一改为 `${VAR}` 引用，通过 `.env` 文件注入（不提交） | 1h |
| 0.5 | **管理员默认密码** | 默认密码过弱 | 首次启动强制修改；或改为随机生成并输出到日志 | 0.5h |
| 0.6 | **注册接口防滥用** | 无验证码/频率限制 | 对 `/auth/register` 增加 IP 级别滑动窗口限流（每分钟 3 次） | 1h |

---

## Phase 1：核心闭环（前后端联动 + 可演示）

> **目标**：用户可完成 注册→登录→添加 Key→发起聊天→查看用量 完整流程。

### 1.1 前端路由 & API 层规范化

| # | 任务 | 方案 | 估时 |
|---|------|------|------|
| 1.1.1 | 删除死代码 | 移除 `App.tsx`、`Usage.tsx`、`Dashboard.tsx`、`Billing.tsx`、`ChatTest.tsx`、`layouts/DashboardLayout.tsx`、`api/` 残留文件 | 0.5h |
| 1.1.2 | 统一 API 模块 | 所有 API 调用收敛到 `services/modules/` 目录下，每个模块一个文件（authApi / keysApi / statsApi / billingApi / adminApi） | 2h |
| 1.1.3 | 补全 API 路径 | 确保所有调用使用 `/api/` 前缀（dev 走 Vite proxy，prod 走 nginx） | 0.5h |

### 1.2 前端功能修复

| # | 任务 | 方案 | 估时 |
|---|------|------|------|
| 1.2.1 | KeysManagement 错误状态 | 添加 error/loading/empty 三态展示，区分"加载失败"与"暂无数据" | 1h |
| 1.2.2 | PlansPage 接入真实 API | 替换 `CURRENT_PLAN_ID = 1` → 调用 `/api/plans/current` | 1h |
| 1.2.3 | UsageDashboard 趋势图增强 | 图表同时展示 `calls`（调用次数）和 `tokens`（Token 消耗）双指标 | 1.5h |
| 1.2.4 | 侧边栏添加管理员入口 | 管理员用户显示"管理后台"导航项，跳转 `/admin` | 0.5h |
| 1.2.5 | 全局 Loading/Error 组件 | 提取可复用的 `LoadingSkeleton` 和 `ErrorFallback` 组件 | 1h |

### 1.3 后端功能补全

| # | 任务 | 方案 | 估时 |
|---|------|------|------|
| 1.3.1 | 添加 Alembic 自动迁移 | `main.py` lifespan 中检测并执行 `alembic upgrade head`（替代 `create_all`） | 2h |
| 1.3.2 | JWT 续期机制 | 添加 `/auth/refresh` 端点，在 token 过期前 5 分钟前端自动续期 | 2h |
| 1.3.3 | API Key 列表分页 | `list_keys` 接口添加 `skip`/`limit` 参数，避免大 key 集解密超时 | 1h |

---

## Phase 2：功能补全（商业化必需）

> **目标**：管理员可用后台，用户可完成支付升级。

### 2.1 管理员后台

| # | 任务 | 方案 | 估时 |
|---|------|------|------|
| 2.1.1 | 用户管理页面 | 列表（搜索/分页）、详情（配额/角色）、编辑（修改配额/封禁）、删除 | 4h |
| 2.1.2 | API Key 管理页面 | 全局 Key 列表（按用户筛选）、启用/禁用/删除操作 | 2h |
| 2.1.3 | 仪表盘统计 | 总用户数、总调用量、今日调用量、活跃 Key 数 | 2h |

### 2.2 支付 & 计费

| # | 任务 | 方案 | 估时 |
|---|------|------|------|
| 2.2.1 | 支付流程对接 | PlansPage "选择套餐"→ 创建订单 → 跳转支付宝 → 回调确认 → 升级套餐 | 6h |
| 2.2.2 | 订单管理 | 用户端：订单列表/状态；管理端：订单列表/手动确认 | 3h |
| 2.2.3 | 配额不足通知 | 剩余配额 < 10% 时 API 响应增加 `X-Quota-Remaining` header，前端弹窗提示 | 1h |

### 2.3 用户体系

| # | 任务 | 方案 | 估时 |
|---|------|------|------|
| 2.3.1 | 注册验证 | 添加邮箱字段，发送验证链接（SMTP）；或初期使用验证码 | 3h |
| 2.3.2 | 密码重置 | `/auth/forgot-password` + `/auth/reset-password` 端点 | 2h |

---

## Phase 3：生产就绪

> **目标**：可安全部署到生产环境，承受真实流量。

### 3.1 数据库 & 迁移

| # | 任务 | 方案 | 估时 |
|---|------|------|------|
| 3.1.1 | 生成初始 Alembic 迁移 | `alembic revision --autogenerate -m "initial"` 生成完整 schema 迁移 | 1h |
| 3.1.2 | 数据库备份策略 | cron job 每日 `pg_dump` + 远程存储（S3/OSS） | 2h |

### 3.2 监控 & 告警

| # | 任务 | 方案 | 估时 |
|---|------|------|------|
| 3.2.1 | 接入 Prometheus + Grafana | 利用已有 `docker-compose.monitoring.yml`，添加 FastAPI metrics 端点 | 3h |
| 3.2.2 | 异常告警 | API 错误率 > 5%、断路器触发、Redis/DB 连接失败 → 钉钉/邮件通知 | 2h |

### 3.3 性能 & 容灾

| # | 任务 | 方案 | 估时 |
|---|------|------|------|
| 3.3.1 | 压测 & 优化 | locust 压测（已有 `locustfile.py`），找出瓶颈 → 优化 DB 连接池 / Redis 缓存 | 4h |
| 3.3.2 | LoadBalancer 真轮询 | 使用 Redis INCR 维护跨请求计数器，实现真正的 round_robin | 1h |
| 3.3.3 | Admin 鉴权中间件优化 | 去掉 `AdminAuthMiddleware` 中的独立 DB 查询，改为从 request.state 读取（由 deps 注入） | 1h |

---

## Phase 4：体验优化（持续）

> **目标**：打磨产品细节，提升用户满意度。

| # | 任务 | 方案 | 估时 |
|---|------|------|------|
| 4.1 | 前端骨架屏 | 关键页面（Dashboard、Keys、Plans）添加 Skeleton 加载态 | 2h |
| 4.2 | 暗色模式完善 | 检查所有页面 dark mode 样式一致性 | 2h |
| 4.3 | API 文档 | FastAPI 自动生成的 `/docs` + 补充中文说明和示例 | 1h |
| 4.4 | CLI/SDK 文档 | `legacy/` 目录下的 CLI 工具使用文档 | 2h |

---

## 依赖关系

```
Phase 0 (安全) ── 无依赖，立即执行
     │
     ▼
Phase 1 (闭环) ── 依赖 Phase 0 完成
     │
     ▼
Phase 2 (功能) ── 依赖 Phase 1.1~1.2 完成（前端基建就绪）
     │              支付对接依赖 1.3.1（DB 迁移就绪）
     ▼
Phase 3 (生产) ── 依赖 Phase 1~2 完成
     │
     ▼
Phase 4 (体验) ── 可与 Phase 3 并行
```

---

## 里程碑

| 里程碑 | 验收标准 | 预计日期 |
|--------|----------|----------|
| **M0: 安全达标** | 仓库中无任何密钥明文；`.env` 不在 Git 跟踪中 | +1 天 |
| **M1: 核心可演示** | 注册→登录→添加 Key→发起聊天→查看用量 全流程通过 | +5 天 |
| **M2: 功能完整** | 管理员可管理用户/Key；用户可支付升级套餐 | +12 天 |
| **M3: 生产就绪** | 压测 100 QPS 无异常；监控告警就位；备份策略生效 | +17 天 |
| **M4: 正式上线** | 完成 Phase 4 核心项；域名/SSL 配置完毕 | +20 天 |

---

> **最后更新**：2026-06-11
> **审计基准**：commit `a3ad582` on branch `main`
